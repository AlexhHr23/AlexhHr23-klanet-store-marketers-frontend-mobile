import 'package:flutter/material.dart';

class CardProductStore extends StatelessWidget {
  const CardProductStore({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}